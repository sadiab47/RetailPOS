import { Injectable } from '@nestjs/common';

@Injectable()
export class DatabaseService {
  constructor() {
    // Database service placeholder
    // Will be configured to connect to MySQL retailpos database
    console.log('Database service initialized');
  }
}
