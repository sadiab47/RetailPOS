# Database Documentation

## Database
The project uses MySQL with an initial schema for users, categories, products, suppliers, sales, and sale_items.

## Setup
1. Start MySQL in XAMPP.
2. Create the database using the SQL in database/schema.sql.
3. Seed initial data with database/seed.sql.
